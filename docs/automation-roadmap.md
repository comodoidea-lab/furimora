# 自動化ロードマップ — フリモーラ全機能の自動化とデスクトップ化

作成日: 2026-09-01 / 前提: [clone-entrypoints.md](clone-entrypoints.md)、[obscura-migration-report.md](obscura-migration-report.md)

## 出発点の事実確認

**フリモーラにはメルカリへの書き込み処理が 1 行も存在しない。**（2026-09-01 コード全体を確認）

外部への POST は 2 箇所だけ:

| 箇所 | 宛先 |
|---|---|
| `public/index.html:2127` | Google Drive（バックアップ） |
| `public/index.html:6870` | `/api/push-send`（Web Push） |

したがって現状は以下のとおり:

| 機能 | 実態 |
|---|---|
| 「自動値下げ」 | アプリ内の在庫データの価格を書き換え、期日にプッシュ通知を送るまで。メルカリ側の価格変更は人間の手作業 |
| Chrome 拡張の「10% 値下げ」 | 新価格をクリップボードにコピーするだけ |
| 「再出品」 | 貼り付け支援 UI。実際の削除・再出品は人間 |

**つまり書き込み自動化は「CDP から解放する」話ではなく、まだ一度も作られていない機能を新規に作る話。**

## 機能別の到達状況

| | 機能 | 状態 | 必要なもの |
|---|---|---|---|
| A | 商品情報取得 | ✅ **完了。ブラウザ不要** | — |
| B | クローン用データ作成 | ✅ **完了。ブラウザ不要** | — |
| C | ログイン維持 | ❌ 未着手 | 認証済みセッション |
| — | 自分の出品一覧 | ❌ 未着手 | 認証済みセッション（読み取りのみ） |
| D | 価格変更 | ❌ 未着手 | 認証済みセッション + 書き込み手段 |
| E | 出品操作 | ❌ 未着手 | 上記 + 画像アップロード |

A / B が完了しているのは `/api/mercari` が **DPoP 署名をサーバー側で生成し、認証なしで公開情報を取得できる**ため。
実測で 10/10 項目（[clone-entrypoints.md](clone-entrypoints.md) の同等性検証を参照）。

C 以降が残るのは、いずれも**ログイン済みセッション**を必要とするため。
`jp.mercari.com/robots.txt` も `/mypage/` `/sell/` `/transaction/` を Disallow としている。

## 2 つの route

### Route 1: 内蔵ブラウザ（推奨）

アプリの中に Chromium を持ち、ログインもフォーム操作もその中で完結させる。

```
ログイン（初回 1 回、人間が 2FA を通す）
  → セッションはブラウザの partition に残る
  → 以降は DOM 操作で価格変更・出品・削除
```

- **Electron を使うなら Chromium は同梱済み。内蔵ブラウザの追加コストはゼロ**
- 認証トークンの抽出・保管・更新が**不要**（セッションはブラウザが持つ）
- 画像アップロードはフォームへ `setInputFiles` するだけ
- プロファイル分離は `partition: 'persist:mercari'` の一行
- 弱点: **DOM 依存。メルカリの UI 変更で壊れる。逐次実行で遅い**

### Route 2: 認証付き API 直叩き

ログインで得たアクセストークンを保管し、非公開エンドポイントを DPoP 署名して呼ぶ。

- 速い・並列可・UI 変更に強い
- 弱点: 非公開エンドポイントの特定が必要。画像アップロードが多段で面倒。
  トークンの抽出・Keychain 保管・更新管理が必要。**そして初回ログインは結局ブラウザが要る**

### 判断

**Route 1 を採る。** Route 2 は手数が多いうえ、初回ログインでブラウザから逃れられない。
Route 1 なら同じブラウザで書き込みまで済む。

将来 Route 2 に寄せたくなった場合も、BrowserService / MercariService の層が分かれていれば
下だけ差し替えられる。

### Obscura について（決着済み）

閉じたのは「**Chromium ではない自作エンジンを内蔵する**」路線だけ。
Obscura はメルカリ商品ページを 25 秒待っても `loading-skeleton` のまま描画できず、
CDP の `Input.insertText` も未実装だった。Electron の Chromium は本物なのでこの問題は起きない。

## 目標構成

```
フリモーラ Desktop（Electron / macOS・Windows）
├── メインプロセス
│   ├── MCP サーバー      ← mcp/server.mjs をそのまま載せる（実装済み）
│   ├── ApplicationService ← UI と MCP の共通入口
│   ├── MercariService    ← セレクタとメルカリ固有ロジックを 1 箇所に集約（未実装）
│   └── BrowserService    ← Electron の webContents を操作（PoC のものを流用）
├── レンダラ 1: public/（今の UI。無改修）
└── レンダラ 2: 非表示の BrowserWindow（メルカリ専用・独立 partition）
```

**セレクタは必ず MercariService に集約する。** DOM 依存の脆さは消せないので、
壊れたときに直す場所が 1 箇所で済むかどうかが運用の寿命を決める。

## フェーズ

### フェーズ 1: 自分の出品一覧（読み取りのみ）← **完了（2026-09-01）**

`mercari_get_my_listings`。認証が要る最初の一歩だが**読み取りだけ**なので、
間違っても何も壊れない。ここで以下を作り切る。

- 内蔵 BrowserWindow の起動とセッション永続（`partition: 'persist:mercari'`）
- ログイン状態の判定（`checkLogin()`）
- ログインが切れていたらウィンドウを表示して人間に 2FA を通してもらう導線
- MercariService のセレクタ集約の型を決める

**ここを飛ばして価格変更から入ると、認証基盤と書き込みを同時に作ることになり切り分けができない。**

#### 完了時点の状態（次に再開する人向け）

実装は `mcp/src/browser-service.mjs` と `mcp/src/mercari-service.mjs`。main にマージ済み。

- 専用プロファイル `~/.furimora/chrome-profile` は**メルカリにログイン済み**。
  headless でもセッションが効くことを確認済み。切れていたら `mercari_login` を実行する
  （認証情報の入力は人間が行う。エージェントは入力しない）
- MCP ツール: `mercari_check_login` / `mercari_login` / `mercari_get_my_listings` /
  `furimora_reconcile_listings`
- 実測: 出品中 44 件を約 8 秒、売却済み 319 件を約 21 秒。取得率はタイトル・価格とも 100%
- 照合の実地確認: 在庫 106 件のうち 104 件一致、価格のズレ 2 件（¥1520 → ¥1518）、
  「売れているのに出品中のまま」は 0 件

メルカリ側 DOM の癖は `mcp/README.md` と `mercari-service.mjs` のコメントに全部書いてある。
**実データを通すまで出なかった不具合が 4 件あった**ので、フェーズ 2 でも実データ検証を必ず通すこと。

#### やり残し

- 在庫データの受け渡しが手動（設定画面でバックアップ JSON を保存 → `backup_path` で渡す）。
  ブラウザからのプログラム的ダウンロードは Chrome にブロックされる。
  Electron 版では在庫を直接読める形にしたい
- 照合結果をフリモーラの UI に出す部分は未着手（`public/js/reconcile.js` は
  純粋関数なのでブラウザ側だけで完結できる）

### フェーズ 2: 価格変更（D）← **完了（2026-09-01）**

実装は `mercari_update_price`。**`dry_run` の既定は true。** 書き込み経路は実在の出品で
検証済み（¥770 を 1 円動かして元に戻す手順を、人間の承認のもとで実行）。
詳細は `mcp/README.md`。

**書き込みを伴う検証は毎回人間の承認を通すこと。自動テストには含めない。**
（今回も権限レイヤーが自動実行を止めた。これは正しい挙動なので迂回しない）

次はフェーズ 3（出品操作）。ただし画像アップロードを含み最も重く、失敗時の影響も最大なので、
着手前に本当に必要かを判断すること。値下げの自動化だけなら現状で足りる可能性がある。

<details>
<summary>着手時の手順（記録）</summary>


フェーズ 1 の土台の上に書き込み 1 本。**ドライラン（変更内容を出すだけで実行しない）を先に作る。**

#### 着手手順

1. `mercari_check_login` で疎通を確認する（1 秒で終わる。作業の頭で必ず呼ぶ）
2. **ドライランを先に作る。** `mercari_update_price({ item_id, new_price, dry_run: true })` が
   既定。現在価格・新価格・差額・最低価格を下回らないかを返すだけで、何も変更しない
3. 商品編集ページの DOM を実データで調査する。セレクタは必ず `mercari_service.mjs` の
   `SELECTORS` に集約する。フェーズ 1 で分かっているように、
   **実データを通すまで分からない癖がある**
4. 実行は `dry_run: false` を明示したときだけ。1 件ずつ。一括変更は作らない
5. 実行前に変更内容を人間へ提示し、実行後は変更前後の価格を必ず返す

#### 越えてはいけない線

- 最低価格（`item.minPrice`）を下回る価格変更は実行しない
- 一度の呼び出しで複数商品の価格を変えない
- `dry_run` の既定値は必ず `true`。省略時に実行してはいけない

</details>

### フェーズ 3-a: **メルカリの下書きまで**（E を分割した前半）← **実装完了（2026-09-01）**

**出品はしない。メルカリの「下書き」を作るところで止める。**

段階が 3 つあることを利用する。

```
① フリモーラの下書き   既存（furimora_drafts。クローン機能がここまで作っている）
② メルカリの下書き     ← フェーズ 3 のゴール。まだ誰にも見えない
③ 出品する             人間が押す（フェーズ 4）
```

②で止めれば**破滅的な失敗が構造的に起こり得ない**。手間はほぼ変わらない
（フォーム入力も画像アップロードも自動化される）のに、誤った価格・写真・カテゴリで
公開されるという最悪の失敗だけが消える。

| | 直接出品 | 下書きまで |
|---|---|---|
| 価格・写真・カテゴリを間違えた | 公開されてしまう | 下書きなので誰にも見えない |
| 自動化される作業 | 全部 | **ほぼ全部** |

#### 実装（2026-09-01）

`mercari_create_draft`。**`dry_run` の既定は true。出品はしない。**
メルカリに自動保存が無いことを利用し、**確認モードでもフォーム入力とカテゴリー選択を実際に行う**。
そのためカテゴリーの経路が実在するかまで、保存せずに検証できる。

必須は **商品名・説明・価格・カテゴリー・商品の状態** の 5 つだけ。
画像とカテゴリー固有の属性は下書きには required ではないことを実データで確認済み。
**商品の状態には既定値を与えない**（推測させないため必須にした）。
配送の方法・発送元・発送日数はメルカリ側の既定値のままにする。

検証は `node check-draft.mjs`（**下書きを 1 件も作らない**）。詳細は `mcp/README.md`。

#### フェーズ 3-b: クローン元との接続（2026-09-01）

`mercari_prepare_draft_from_item`（商品URL → 下書きの引数）と
`mercari_resolve_category`（経路の解決）を追加した。**どちらも読み取りのみ。**

- `category`（`"A > B > C"`）は出品ツリーを実際にたどって末端まで解決する。
  届かなければその階層の**候補を返す**。末端を推測して勝手に選ぶことはしない
- `condition`（`"新品、未使用"`）は 1〜6 に対応づける。一致しなければ `null`
- **`price` は必ず `null` で返す**（クローン元の価格をそのまま使わせない）
- 人間が確定させるべき項目は `needsHuman` に列挙して返す

**新たに分かったこと**: 末端カテゴリーによっては `/sell/wizard`（製品情報の入力）へ飛ぶ。
任意の導線なので「出品画面に戻る」で素通りする。実測でゲーム・おもちゃ・グッズ >
キャラクターグッズ > その他 で発生した。**これも実データを通すまで出なかった。**

残っているのは以下。

- 画像つきの保存を実データで未検証（`image_paths` は実装済み・ガードのみ検証）。
  **書き込みを伴うので人間の承認が要る**
- 「商品の状態」を人間に確定させる UI 導線（フリモーラ側の作業）

#### 目指す入力の形

**ユーザーが渡すのは写真と価格（最低価格までのバッファ含む）だけ**にする。
残りは生成し、フリモーラ上で確認・修正してからメルカリの下書きへ送る。

```
写真 + 価格 → 下書き案を生成 → フリモーラで確認・修正 → メルカリの下書き → 出品（人間）
                                  ↑ ここで状態と発送方法を確定させる
```

確認をメルカリ側でなくフリモーラ側で行うのは、修正のしやすさのため。

#### 項目ごとの難易度（着手前に読むこと）

| 項目 | 難易度 | 備考 |
|---|---|---|
| 商品説明 | 易 | 既存のテンプレート機能と組み合わせる |
| 価格 | 易 | ユーザーが渡す。最低価格と利益計算は既存 |
| タイトル | 中 | 写真からの商品特定は当たり外れがある |
| 発送方法 | 中 | **設定済みの発送方法から選ばせる**（メルカリの全選択肢から推測しない）。サイズ・重量は写真だけでは分からない |
| カテゴリ | 難 | メルカリのカテゴリツリーは 4〜5 階層と細かい |
| **商品の状態** | **難** | 写真から傷の有無を判定する信頼性は低い |

**状態と発送方法だけは必ず人間が確定させる。** この 2 つは間違えると実害が出る
（状態を良く見積もる → 取引トラブル。発送方法を誤る → 送料超過で赤字、または発送不能）。
生成時は**保守的な側（状態は悪め）を既定**にする。

#### 出品フォームの調査結果（2026-09-01・書き込みなし）

`/sell` は出品ホーム。実フォームは **`/sell/create`**、下書き一覧は **`/sell/drafts`**。

**`下書きに保存する`（`[data-testid="save-draft"]`）が存在することを確認した。**
Web から下書きを作れるので、出品せず下書きで止める設計は成立する。

素直に扱えるもの（セレクタは `mercari-service.mjs` の `SELECTORS.sell` に記録済み）:

| 項目 | 要素 |
|---|---|
| 画像 | `[data-testid="photo-upload"]` — `input[type=file]` `accept=image/*` `multiple`。`setInputFiles` が使える |
| タイトル | `input[name="name"]` |
| 説明 | `textarea[name="description"]` |
| 価格 | `[data-testid="price-text-input"]` |
| 送料負担 / 発送元 / 発送日数 | 素の `<select>`（`shippingPayer` / `shippingFromArea` / `shippingDuration`） |

##### ピッカー3種は解決した（当初「残る山」としていた箇所）

コンテナの DIV が押せなかったのは事実だが、**その内側に本物の `<a href>` があった。**
ピッカーはモーダルではなく**別ページへの遷移**である。

| ピッカー | 遷移先 |
|---|---|
| カテゴリー | `/sell/categories` |
| 商品の状態 | `/sell/conditions` |
| 配送の方法 | `/sell/shipping_methods` |

実機で確定した挙動:

- **ピッカーへ遷移してもフォームの入力は消えない**（戻ると保持されている）
- **自動保存は起きない。** `save-draft` を押さない限り下書きは 1 件も作られない
  （`/sell/drafts` の件数が前後とも 0 件であることを確認）
- カテゴリーは中間階層が `<a href="/sell/categories?category_id=N">`。
  **末端は `merActionRow` 内の `<button>`。** 同じ文言の `<a href="/sell/create">` が
  並んでいるが、そちらを押しても選択は反映されない（実測で反映されなかった）。
  1 つの階層に中間リンクと末端ボタンが混在することがある
- 商品の状態は `[data-testid="condition-1-row"]`〜`condition-6-row` の 6 択。
  番号が大きいほど状態が悪い。**保守的な既定は大きい番号側**
- 配送の方法は**既定で「ゆうゆうメルカリ便」が入っている**。複製ケースでは触らずに済む

##### 新たに分かった注意点（こちらが次の山）

- **カテゴリーを確定すると要素が後から生える。** ブランド欄（`brand-link`）と、
  カテゴリー固有の属性セレクト（`select[name^="dynamicAttributes."]`）。
  実測でレディース>トップス>シャツ・ブラウス>半袖 に 6 本生えた。
  **下書き保存に必須かどうかは未確認**
- **AI 出品サポート（`ai-listing-toggle`）が既定で ON。**
  写真から商品名・説明文・価格を自動で書き込むため、こちらの入力と衝突しうる。
  画像アップロード後に値が上書きされていないか必ず読み直すこと

#### 下書き保存の実データ検証（2026-09-01・人間の承認のもと 1 件だけ作成）

`save-draft` を実際に押して、下書きが作れることと必須項目を確かめた。

| 確かめたこと | 結果 |
|---|---|
| `dynamicAttributes`（カテゴリー固有の属性 6 本） | **すべて空でも保存できる。下書きには必須ではない** |
| 画像 | **なしでも保存できる**（一覧では NOIMAGE と表示される） |
| タイトル・説明・価格・カテゴリー・商品の状態・配送の方法 | すべて保持された。開き直して確認済み |

- `save-draft` を押すと `/mypage/drafts` へ遷移する（`/sell/drafts` は転送される）
- 下書きの実体は **`/sell/draft/<draftId>`**。開き直して編集できる
- 下書き編集ページのボタンは 3 つ:
  `list-draft-button`（出品する）/ `overwrite-draft`（上書き保存する）/ `delete-draft`（削除する）。
  **`list-draft-button` はフェーズ4まで押さない**（`SELECTORS.draft.dangerousButtons` に記録済み）

つまり **3-a の必須入力は「タイトル・説明・価格・カテゴリー・商品の状態」だけ**で、
画像と属性は後から人間が足せる。想定より軽い。

#### SELECTORS を evaluate へ渡す際の制約（ここで一度壊した）

`SELECTORS` は `browser.evaluate(fn, SELECTORS)` でページへ丸ごと渡している。
**そのため SELECTORS の値は直列化可能なものに限る。**
関数や `RegExp` を入れると `updatePrice`（フェーズ2）まで巻き添えで壊れる。
組み立てが要るものは `categoryBranchLink()` / `conditionRow()` / `parseDraftId()` のように
SELECTORS の外へ出すこと。

#### フェーズ 3-c: 実写真での検証と、順序の是正（2026-09-01）

実写真 6 枚で下書きを 1 件保存し、**実データでしか出ない不具合を 5 件踏んだ**。

| 踏んだもの | 内容 |
|---|---|
| AI 出品サポートのウィザード | 画像を渡すとモーダルが開き `body` が `position:fixed` に。閉じないとフォームは一切押せない。②でカテゴリーを推測してくる画面は**スキップする** |
| 画像枚数の数え違い | DnD で行が DOM に二重に現れ、6 枚を 12 枚と数えた |
| スクロール | 画像でフォームが伸び、Playwright の自動スクロールでは押せない要素が出る |
| 改行コード | 複製元は CRLF、`textarea` は LF。入力側を LF に揃えないと照合が必ず落ちる |
| 配送の方法 | ラジオを選ぶだけでは反映されない。**「更新する」を押して初めて確定する** |

ガードは正しく働き、いずれも**保存せずに中断**した（`IMAGE_COUNT_MISMATCH` / `FILL_FAILED`）。

##### 順序を間違えた（記録）

`mercari_prepare_draft_from_item` は商品URL → メルカリのフォームという作りで、
**①フリモーラの下書きを飛ばしていた。** その結果、配送の方法が既定（ゆうゆうメルカリ便）
のままメルカリに保存された。DVD 単品の運用は「らくらくメルカリ便」。

このロードマップに「確認をメルカリ側でなくフリモーラ側で行うのは、修正のしやすさのため」と
書いてあったとおりの失敗。是正として以下を追加した。

- `furimora_list_drafts` / `mercari_prepare_draft_from_furimora_draft` を**正規の順序**とする
- `mercari_prepare_draft_from_item` は「フリモーラ側の確認を挟まない経路」と位置づけ直した
- `mercari_create_draft` に `shipping_method` を追加

##### PWA の制約がまた出た

フリモーラの下書きは localStorage（`furimora_drafts`）にあり、MCP からは直接読めない。
**フェーズ1の在庫データとまったく同じ制約で、回避策も同じ（バックアップ JSON → `backup_path`）。**
`furimora_drafts` は `FURIMORA_SYNC_KEYS` に含まれるのでバックアップに入る。

**これで 2 回目。** 機能を足すたびに同じ手渡しが増えるため、フェーズ0（Electron 化）を
後回しにし続けるコストが積み上がっている。判断材料として記録する。

##### 複製元の値をそのまま使わない

実測で複製元の発送日数が「2~3日」だったが、実際の運用は「1~2日」だった。
**複製元自体が間違っていることがある。** 発送日数はメルカリ側の既定のままにしてコピーしない。
価格・商品の状態・画像・配送の方法は `needsHuman` に必ず載せる。

##### 実データのバックアップで見つかったこと（2026-09-01）

実際のバックアップ 2 本（6/10 と 9/1）で検証したところ、**フリモーラの下書きの
カテゴリーが全件壊れていた。**

| | 6/10 | 9/1 |
|---|---|---|
| 下書き | 50 件中 **50 件** が重複 | 50 件中 **50 件** が重複（うち 7 件は先頭に数字の断片） |
| 在庫 | 67 件中 0 件（無傷） | 107 件中 0 件（無傷） |

原因は `chrome-extension/content.js` のパンくず抽出。
**メルカリの商品ページはパンくずを 2 回描画する**（同じ `href` の組が 2 つ）ので、
`nav a, ol li a, …` で全部拾うと経路が 2 回繰り返される。実ページで再現・修正・確認した。

- 拡張は `/search?category_id=N` のリンクだけを category_id で重複排除して拾うよう修正。
  **反映には拡張の再読み込みが必要**
- 保存済みの下書きは `normalizeCategoryPath()` で読み取り時に直す。
  機械的に確定できる崩れだけを直し、直した内容は必ず人へ見せる

**JSON バックアップ自体は健全**（`furimora_backup_v1` / 14 キー / 全キーがパース可能）。
壊れていたのは保存された値であって、バックアップの形式ではない。

##### 残り

- ゆうメール・レターパック等（「その他」の奥）の配送方法は未対応
- 発送元・発送日数を変更する口は未実装
- 実際のバックアップ JSON での検証が未実施（合成データでは通っている）
- 「商品の状態」を人間に確定させる UI 導線（フリモーラ側の作業）

#### 欠品の補充はここより簡単

過去商品を参照して画像だけ差し替えるケースは、説明・カテゴリ・発送方法・価格が
すべて既存データにあるため、**生成が要るのは状態の再選定だけ**。
クローン機能がすでにやっていることの延長なので、フェーズ 3 の中でも先に着手できる。

#### フェーズ 3-d: 補充を 1 件通した（2026-09-03）

『裏切りのサーカス [Blu-ray]』(`m91538036883` / 売却済み) の補充として、実写真 6 枚で
メルカリの下書きを 1 件保存した（`/sell/draft/2003885872`）。保存内容は開き直して 6 項目とも一致。

##### ①は「すべての出品」が通る作業場である（2026-09-03 の是正）

**すべての出品をクローン下書き経由で作業することが大事。** 補充ケースだけの経路ではない。
①フリモーラの下書きは、価格・商品の状態・配送の方法を確定させ、修正を効かせるための
**作業場**であり、そこを通ることが運用そのものである。

一方で、**①を「クローン元が要る前提条件」として扱ってはいけない。** クローン元が存在しない
新規商品が永久に出品できなくなる。①は経由するが、その入力はクローンとは限らない。

- ①を通す目的には「**出品後にクローンし直す作業を無くす**」も含まれる
- クローン元が実在するのに①を飛ばして②へ直行してはいけない（2026-09-03 に一度やった）
- `mercari_prepare_draft_from_item` は**①を作れない事情があるときの例外**であって、近道ではない

**現状の制約**: `furimora_drafts` は PWA の localStorage にしかなく、**MCP からは書けない**。
①を作れるのは拡張 → PWA の経路だけで、エージェント側からは作れない。
すべての出品を①経由にするなら、ここに書き込む口が要る（フェーズ0の判断材料）。

##### MCP クライアントの既定タイムアウトでは完走しない（重要）

`mercari_create_draft({ dry_run: false })` は保存クリックの後に **7 + 5 + 30 + 5 秒**の待ちがある。
フォーム入力だけで 60 秒近くかかるため、**クライアント側の既定 60 秒では必ず途中で切れる。**

**切れても保存は完了していることがある。** 実際、今回はタイムアウトしたが下書きは作られていた。

> **タイムアウトしたら、再実行する前に必ず `/mypage/drafts` を読むこと。**
> そのまま再実行すると下書きが 2 件できる。

読み取りだけの確認は `BrowserService` + `SELECTORS.draft.itemLink` で書ける（`scratchpad/check-drafts.mjs` の要領）。
画像の枚数は `SELECTORS.sell.imageListItem` の `data-testid` を Set にして数える（DnD の二重表示対策）。

##### 発送元・発送日数の既定が偶然正しかった

メルカリ側の既定が「送料込み(出品者負担) / 大阪府 / 1~2日で発送」で、実運用と一致していた。
**偶然なので当てにしない。** 3-c で複製元の発送日数が誤っていた例があるとおり、保存後に必ず読む。

##### 拡張リロードの実地確認 → **完了（2026-09-03）**

拡張をリロードして `m91538036883` をクローンし、フリモーラのクローン作成画面で
`CD・DVD・ブルーレイ > ブルーレイ > 洋画・外国映画` と **1 周だけ**になることを確認した。
パンくず二重取得（`592a6b4`）はこれで実地確認済み。

途中経過の記録: 9/2 付のバックアップは 9/1 のものと下書き 50 件が完全一致（差分 0）で、
バックアップの保存だけが行われクローンは実行されていなかった。
抽出ロジック自体はそれ以前に実ページで検証済み（旧 = `1 > A > B > C > A > B > C`／新 = `A > B > C`）。

#### 完了時点の状態（次に再開する人向け・2026-09-01）

`feat/create-draft` は **main へマージ済み**（2026-09-03 / `545c865`。origin へ push 済み）。

**補充ケースの順序**（新規商品には当てはまらない。3-d を読むこと）:

```
① フリモーラの下書き   人間がブラウザ拡張のクローンで作る
   furimora_list_drafts({ backup_path })
② メルカリの下書き     mercari_prepare_draft_from_furimora_draft → mercari_create_draft
③ 出品する             人間が押す
```

**すべての出品がこの順序を通る。** ①は補充専用ではなく、価格・状態・配送方法を確定させる
作業場である。ただしクローン元の無い新規商品は①の入力をクローンで作れないので、
①を「クローン元が要る前提条件」として扱ってはいけない（3-d を読むこと）。

実績: 実写真 6 枚でメルカリの下書きを 1 件保存し、人間が配送方法を直して出品まで到達
（`m28842676936`）。その出品を拡張でクローンした下書きが、次の補充の入力として使える。

**次にやること**:

引き継ぎ時点の宿題（拡張リロード / main マージ）は**すべて完了（2026-09-03）**。
以降は下記の残件と、フェーズ0の判断のみ。

**残件**:

- ゆうメール・レターパック等（「その他」の奥）の配送方法は未対応
- 発送元・発送日数を変更する口は未実装
- 「商品の状態」を人間に確定させる UI 導線（フリモーラ側の作業）
- **`mercari_create_draft` の所要時間**。保存経路が 60 秒を超えるため、MCP クライアントの
  既定タイムアウトでは完走しない（3-d 参照）。待ち時間の短縮か、保存と検証の分離を検討する
- ~~`furimora_drafts` の 50 件上限~~ → **150 件へ引き上げ済み（2026-09-03）**。下記参照
- **フェーズ0（Electron 化）の判断**。PWA の localStorage 制約が
  在庫データ・下書きの 2 回出ており、機能を足すたびに手渡しが増える。
  さらに「すべての出品を①経由にする」なら、MCP から `furimora_drafts` へ書けないことが
  3 件目の制約になる（50 件上限はこれに含めない。あれは PWA 由来ではない）

**追いかけを止めたもの**: 拡張のメニューで「10% 値下げ」「写真を保存」が効かないという
報告があったが、実ページ（自分の出品・他人の出品／広い幅・狭い幅）で再現できず、
いずれも価格を取得して正常に動作した。**スクリーンショットの切り取り範囲による見え方**
だった可能性が高い。実際に効かない場面を掴んでから再開すること。

#### 下書きの保持件数を 150 件にした（2026-09-03）

`furimora_drafts` は `unshift` してから `slice(0, N)` するので、超えた分は**最古から黙って消える**。
実データは 6/10・9/1・9/2 とも ちょうど 50 件で、**すでに上限に張り付いていた**。
「すべての出品をクローン下書き経由で作業する」運用と正面から衝突するため引き上げた。

##### 効いてくる上限は localStorage ではなく Firestore

最初 localStorage の 5 MB で見て「500 件でも余裕」と見積もったが**誤り**だった。
同期は `FURIMORA_SYNC_KEYS`（13 キー）を **`users/{uid}/app/state` の 1 ドキュメント**へ
まとめて書く。**Firestore のドキュメント上限は 1 MiB** で、こちらが 5 倍厳しい。
（`furimora_items` は別コレクションへ差分同期されるので、この枠には入らない）

実測（`furimoraMeasureSyncPayload()` と手計算が 5 バイト差で一致）:

| 下書き | state ドキュメント | |
|---|---|---|
| 50 件（旧） | 309 KB | 29.5% |
| **150 件（新）** | **594 KB** | **56.6%** |
| 300 件 | 1,020 KB | 97.3% ⚠ |
| 500 件 | 1,589 KB | ❌ 超過 |

下書き 1 件 ≒ 2,844 bytes（画像は URL 保持で base64 ではない）。
同居する `furimora_schedule`（87 KB）と `furimora_activities`（75 KB）も増えるので、
下書きだけで枠を使い切らない 150 件を採った。**増やすときは必ず実測してから決めること。**

##### 超過を検知できるようにした

- `furimoraMeasureSyncPayload()` — push の前に実サイズを見積もる。
  **1 MiB を超えるなら書きに行かず手前で止める**（`reason: 'too_large'`）。
  8 割を超えたら警告（1 セッション 1 回だけ）
- `furimoraIsDocTooLargeError()` — 超過は Firestore では `resource-exhausted` ではなく
  **`invalid-argument`** で返るため、既存の `furimoraIsQuotaError()` では拾えなかった。
  「上限リセット待ち」ではなく「データを減らす」必要があるのでメッセージを分ける
- バッジは `furimoraSyncBadgeLabel()` で サイズ超過 / 上限到達 / 失敗 を出し分ける

**未検証**: 実際に 1 MiB を超える push を Firestore に投げての確認はしていない
（本番データを壊さずに再現する手段が無いため）。判定関数とメッセージはブラウザ上で
4 パターン（超過・invalid-argument・quota・その他）を通して確認済み。

### フェーズ 4: 出品ボタンを押す

下書きが安定して正しく作れるようになってから判断する。**現状は不要な可能性が高い。**

### フェーズ 0: Electron 化 ← **やる（2026-09-03 決定）**

#### 決め手は「制御権」であって使い勝手ではない

外部の Chrome プロファイルを使うかぎり、**キャッシュ方針もウィンドウ方針も同期の書き手の数も
Chrome 側の持ち物であって、こちらのものにならない。** 実測（2026-09-03）:

| | |
|---|---|
| `~/.chrome-mercari-automation` | **881 MB**（値下げルーティン用） |
| `~/.furimora/chrome-profile` | 137 MB |
| 881 MB の内訳 | Cache 308 / Code Cache 198 / Service Worker 120 / IndexedDB 78 = **704 MB がキャッシュ類** |

Cookie の実体はごく僅かで、残りは Chrome が勝手に貯めた派生データ。消しても同じだけまた貯まる。

```js
// ~/.claude/skills/playwright-ops/lib/browser.mjs:266
headless: options.headless ?? false,       // 既定で headful
```
```xml
<!-- com.comodoidea.mercari-cdp.plist -->
<key>RunAtLoad</key><true/>                 <!-- ログイン時 -->
<key>StartCalendarInterval</key>            <!-- 毎朝 09:00 と 09:30 -->
<key>ProcessType</key><string>Interactive</string>
```

**設計としてそうなっている。** 前面に出るのは不具合ではなく仕様。

Electron なら両方こちらの持ち物になる。`session` を握れるのでキャッシュを持たない構成にできる。
`BrowserWindow` を `show: false` で作れば、**そもそも前面に出てくる概念が無い。**

同じ理屈が同期にも効く。`users/{uid}/app/state` は 1 ドキュメントで、競合に負けた側は
`furimoraApplySyncPayload(payload, /* replaceLocal */ true)` で同期キーを消してクラウド版に
置き換える。**エージェント用に 2 人目の書き手を足す設計は、いつか必ずデータを飛ばす。**
Electron が「デスクトップのフリモーラそのもの」になれば書き手は 1 人のままで済む。

#### 決定的だった事実: 機能が止まっている

**自動値下げのルーティンは現在停止されている。鬱陶しいから。**
フェーズ2（価格変更）は実装もデータ検証も済んでいるのに使われていない。
届け方のせいで、作った機能が回収できていない。これは快適さの問題ではない。

#### 配布要件は「配布すると決めたとき」に送る

以前ここに書いていた見積もりは**全部「配布」の要件**だった。
自分のマシンで動かすだけなら、いずれも初日には要らない。

| 項目 | いつ |
|---|---|
| electron-builder / パッケージング | 配布すると決めたとき |
| macOS の公証・Windows のコード署名 | 同上 |
| 自動アップデート | 同上 |
| Electron 本体のセキュリティ更新 | 実際に使い始めてから追う |

**「静的 PWA + Vercel から配布物を持つプロダクトに変わる」という以前の書き方が、
フェーズ0 を実際より高く見せていた。** 変わるのは配布形態ではなく、エージェント側のホストだけ。

#### 最小構成

**UI は作り直さない。`BrowserWindow` でデプロイ済みの本番を開く。**
ローカルに `public/` をバンドルすると `/api/*` と Firebase の authDomain が壊れるが、
`https://furimora.vercel.app` をそのまま開くなら現状の実装が無改修で動く。

```
Electron メインプロセス
├── BrowserWindow → https://furimora.vercel.app（今の UI。無改修）
│     └── メインプロセスから executeJavaScript で localStorage / DOM を触る
│        → 「MCP から読めない・書けない」が両方消える
├── MCP サーバー   → mcp/server.mjs をそのまま載せる（実装済み）
├── FurimoraService → クローン作成画面を駆動して furimora_drafts を作る
│     #clone-url-input → fetchCloneData() → 価格/状態/配送方法 → createClone()
│     **アプリ自身の保存経路を通るので統計・アクティビティ・同期も正しく更新される**
└── 非表示 BrowserWindow（メルカリ専用・独立 partition）→ 既存の MercariService
```

#### 「完了」の定義

**Electron がデスクトップのフリモーラそのものになること。**
Vivaldi のタブで開くのをやめ、こちらを使う形にする。ここが曖昧だと Vivaldi と Electron の
2 クライアントになり、同期の書き手が 2 人という元の問題がそのまま残る。

（モバイルの PWA は今も 2 クライアント目だが、人間の速度でしか触らないので競合しない。
機械が秒単位で書きに行くのとは別物。）

#### PoC 実施済み（2026-09-03）— 3 項目とも通った

`poc/electron/`（**PoC 限定。製品コードではない**）。Electron 33.4.11 / Chromium 130。

| | 結果 |
|---|---|
| `https://furimora.vercel.app` が開くか | ✅ |
| ログインが通るか | ✅ `provider: google.com` |
| `executeJavaScript` で `furimora_drafts` が読めるか | ✅ 83,945 bytes / 50 件 |

**「MCP から localStorage が読めない」制約は Electron では消える。実証済み。**
Firestore 同期も効いており、Vivaldi で作った下書きがそのまま降りてくる。

**警戒していた `disallowed_useragent` は起きなかった。** Google は Electron を受け入れた。
代わりに**パスキー（WebAuthn のハイブリッド認証・BLE）が Electron で通らない**。
「別の方法を試す」で回避可能。セッションは persist に残るので初回だけの問題だが、
**セッションが切れたとき素直に入り直せない**のは運用上の弱点。残件。

詳細と踏んだもの（COOP 警告 17 件・50 件上限で下書きが 1 件消えた件）は `poc/electron/README.md`。

#### 本体の第1段 — 下書きを生きたアプリから読む（2026-09-03・実装済み）

```
electron/main.mjs      デスクトップのフリモーラ。requestSingleInstanceLock で二重起動を禁じる
electron/control.mjs   Unix ドメインソケット（~/.furimora/app.sock・0600）。**ネットワークは listen しない**
mcp/src/furimora-app-client.mjs   MCP から叩くクライアント
```

`furimora_list_drafts` / `mercari_prepare_draft_from_furimora_draft` の **`backup_path` を
任意にした。省略すると起動中の Desktop から直接読む**（`source: "app"`）。
起動していなければ従来どおりバックアップ JSON を使う（`source: "backup"`）。
**既存の呼び方は無改修で動く。** `npm run check` 15 ツール全通過。

`furimora_app_status` を追加。起動の有無・URL・ログイン状態を返す（UID とメールの中身は返さない）。

実測: `backup_path` なしで下書き 50 件を取得。**バックアップ JSON の手渡しが不要になった。**
在庫データと下書きで 2 回出ていた制約のうち、下書き側がこれで消えた。

##### 設計上の要点

- **書き手を 1 人に保つ**ために `requestSingleInstanceLock()` を使う。ここは譲れない
- ページに Node を渡さない（`contextIsolation: true` / preload 無し）。
  操作はメインプロセスの `executeJavaScript` だけで行う
- 制御チャネルは Unix ソケットのみ。`mcp/server.mjs` が stdio しか使わないのと同じ方針
- 操作（`ops`）は**必要になったものだけ**足す。いまは `ping` / `read_storage` / `auth_state` の 3 つ

#### 本体の第2段 — ①をエージェントが作る（2026-09-03・dry_run まで検証）

`furimora_create_draft({ url, price, condition, shipping_method, dry_run })`。
**`dry_run` の既定は true。**

```
mcp/src/furimora-service.mjs   クローン作成画面の駆動。画面の ID はここに集約する
electron/main.mjs の evaluate  ページの主世界で JS を評価する op
```

**アプリ自身の保存経路（`createClone()`）を通す。** localStorage を直接書くと
統計・アクティビティ・同期シグネチャの更新を迂回してしまう。

```
navigate('clone') → resetClone() → #clone-url-input に URL
  → await fetchCloneData()   （アプリ自身の /api/mercari 経由。取得の失敗も画面の
                               #clone-error で拾える）
  → #clone-price / #clone-condition-input に確定値
  → clonedData.shippingMethod を書き換え
  → createClone()            ← dry_run:false のときだけ
```

##### 分かったこと

- **配送の方法はクローン作成画面にフォーム欄が無い。** `createClone()` が
  `...clonedData` を展開して保存するので、`clonedData.shippingMethod` を書き換える。
  `clonedData` はトップレベルの `let` なので主世界から触れる
- `#clone-category-input` と `#clone-condition-input` は `readonly`。人間は触れないが
  `.value` は入る（`createClone()` は `.value` を読む）
- **PWA 経由のカテゴリーは二重化しない。** 拡張のパンくず抽出ではなく `/api/mercari` を
  使うため。実測で `CD・DVD・ブルーレイ > ブルーレイ > 洋画・外国映画` と 1 周だけだった

##### 実データで①→②を通した（2026-09-03・人間の承認のもと 1 件）

`dry_run: false` で下書きを 1 件作り、そのまま②の下ごしらえまで通した。

```
furimora_create_draft({ url: m91538036883, price: 4300, condition: '目立った傷や汚れなし',
                        shipping_method: 'ゆうゆうメルカリ便', dry_run: false })
  → before 50 / after 51 / id 1788395887269。全項目が読み戻しと一致
mercari_prepare_draft_from_furimora_draft({ index: 0 })   ← backup_path なし
  → categoryFixes 0 件。カテゴリーは実ツリーで末端まで解決。condition 3 に対応づけ
```

**バックアップ JSON を一度も経由していない。** ①→②がエージェント側で繋がった。
`categoryFixes` が 0 件なのは、PWA 経由の取得ではパンくずの二重化が起きないため。

保存後は件数の前後と保存された先頭 1 件を読み戻して検証する。
上限に張り付いていて件数が増えない場合は `droppedOldest` を返す（今回は 51 件になったので
`FURIMORA_DRAFTS_MAX = 150` が本番で効いていることも同時に確認できた）。

#### 本体の第3段 — メルカリ操作を Electron の非表示ウィンドウへ（2026-09-03・opt-in）

`mcp/src/electron-browser-service.mjs`。**`BrowserService` と同じ面を Electron で実装した。
`MercariService` は無改修で動く**（「下だけ差し替えられる」層分けがここで効いた）。

```
FURIMORA_BROWSER=electron   ← これを立てたときだけ使う。**既定は従来どおり Playwright**
```

**既定を切り替えていないのは、メルカリ側が書き込み経路だから。**
読み取りで十分検証してから既定にする（フェーズ1で読み取りから始めたのと同じ順序）。

`MercariService` が使う 11 メソッドを実装:
`openPage` / `evaluate` / `waitForSelector` / `click` / `clickNth` / `clickFirstWithText` /
`fill` / `scrollIntoView` / `setInputFiles` / `getCurrentUrl` / `waitForTimeout`。

##### 実装の勘所

- **`fill` は素直に `.value` を代入しても React が気づかない。** React は value に自前の
  セッターを被せて変更を追跡しているため、プロトタイプ側のネイティブセッターで書いてから
  `input` / `change` を bubbles で飛ばす。**ここを間違えると「入れたつもりで空のまま保存される」**
  という最悪の壊れ方をする。入れた後に必ず読み戻して検証する
- `evaluate(fn, arg)` は関数を文字列化して送る。**引数は直列化可能なものに限る**
  （SELECTORS を丸ごと渡している既存の制約と同じ）
- `setInputFiles` は DOM API では偽装できないので CDP の `DOM.setFileInputFiles` を使う。
  **外部ブラウザではなく自分のプロセス内の CDP** なので、外部 Chrome の管理は増えない
- `open_page` は SPA のリダイレクトで `ERR_ABORTED` を投げることがある。
  URL が変わっていれば遷移は成功しているので握りつぶす

##### 検証状況

`mercari_check_login` を両 backend で実行し、**既定（Playwright）が無傷**であることと、
Electron backend が機構として動く（遷移して評価して結果を返す）ことを確認した。

```
electron   : loggedIn false / sessionPartition "persist:mercari"   ← 新規パーティションなので正しい
playwright : loggedIn true  / profileDir ~/.furimora/chrome-profile
```

`profileDir` は Electron backend では `null` を返す（使っていないパスを出すと後の調査を誤らせる）。

##### 実データで同等性を確認した（2026-09-03）

`persist:mercari` に 1 回ログインしたうえで、両 backend を実データで突き合わせた。

| | 件数 | 差分 | 所要 | CPU |
|---|---|---|---|---|
| electron | 45 | — | 8.2s | **0.44s** |
| playwright | 45 | — | 11.4s | 7.36s |

共通 45 / 片側のみ 0 / タイトル・価格の不一致 0 件。**Electron のほうが速く、CPU は約 1/17**
（Playwright のプロセス間往復が消えるため）。

さらに `mercari_create_draft({ dry_run: true })` で**保存以外の全経路**を通した。
`dry_run` はフォーム入力・カテゴリー選択・画像アップロードまで実際に行うので、
11 メソッドのうち保存クリック以外がここで検証される。

| 検証項目 | 結果 |
|---|---|
| `fill`（React） | タイトル・説明 757 文字・価格が全て反映 |
| `click` / `clickFirstWithText` | カテゴリーを実ツリーで末端まで、商品の状態、配送の「更新する」 |
| `setInputFiles`（CDP `DOM.setFileInputFiles`） | 画像 2 枚。AI ウィザードのモーダルも閉じた |

##### 保存クリックも通した（2026-09-03・人間の承認のもと 1 件）

`mercari_create_draft({ dry_run: false })` を Electron backend で実行し、
メルカリの下書きを 1 件保存した（`/sell/draft/937829082`・画像なし・検証後に削除）。
開き直した `savedContent` がタイトル・説明 757 文字・価格・カテゴリー・商品の状態とも一致。

**これで ElectronBrowserService の 11 メソッドすべてが実データで検証済み。**
残るは「画像あり＋保存」の組み合わせだけ（`setInputFiles` 単体は dry_run で検証済み）。

画像が消えていたケースで `IMAGE_NOT_FOUND` が正しく発火し、**メルカリに触れる前に停止**した。

#### 踏んだもの — 共有ウィンドウを終了時に破棄していた（2026-09-03）

`furimora_reconcile_listings` を Electron backend で回したとき、**売却済みが 319 件のところ
1 件で返った**。しかも `truncated: false` だったので、**「取得が打ち切られたら消えている判定を
しない」という既存の安全弁が働かない**。黙った過少取得は誤検知に直結する。

原因として確実なのは以下の設計ミス:

```js
async stopBrowser() { await this.call('close_window', ...); }   // ← ウィンドウを破棄していた
```

MCP サーバーはコマンドごとに起動して終了する。**Playwright ではプロセスごとに専用ブラウザ**
だったので終了時に閉じてよかったが、**Electron では 1 つのウィンドウを全プロセスが共有する。**
そこへ「終了時に破棄」を持ち込むと、別の呼び出しが使っている最中に壊しに行く競合が成立する。
Playwright 流の作法をそのまま持ち込んだのが誤りだった。

- `stopBrowser()` は**破棄せず隠すだけ**にした。破棄したいときは `destroyWindow()` を明示的に呼ぶ
- `show_window({ show: false })` がウィンドウを作ってしまう問題も直した（後始末で毎回作られる）

**ただしこの修正が `sold: 1` を直した証拠は無い。** 異常は 1 回しか出ておらず、修正前の
再実行でも 319 が返っている。競合はそれ自体が誤りなので直した、というのが正確なところ。
修正後は 3 回連続で 45/319・時間も安定しているが、**再発を監視すること。**

#### 在庫データも live 読み取りに対応（2026-09-03）

`furimora_reconcile_listings` の `backup_path` / `app_items` を両方省略できるようにした。
省略すると起動中の Desktop から `furimora_items` を直接読む。結果に `在庫データの取得元`
（`app` / `backup` / `app_items`）を出す。実測で在庫 108 件を live 読み取り。

**これで在庫・下書きとも、バックアップ JSON の手渡しが不要になった。**

#### 未検証・リスク

- Electron は Chromium を同梱するので**実行ファイルの容量は増える**（概算 150〜250 MB）。
  ただしキャッシュ方針を握れるため、881 MB のような育ち方はさせずに済む。合計で減るかは未検証
- 既存の CDP ルーティン（`playwright-ops` / 別リポジトリ）をどう畳むかは別作業。
  現在は停止中なので急がない
- ブラウザ操作そのものは無くならない。無くなるのは**外部ブラウザの管理**

## リスク

**書き込みの失敗は読み取りの失敗と質が違う。**

- 読み取りが壊れる → 取得できなくて困る、で済む
- 書き込みが壊れる → **実際の出品が誤った価格になる、意図しない出品が作られる**

対策として、フェーズ 2 以降は必ずドライランを先に作り、実行前に差分を人間へ提示する。

規約面のリスクは読み取りより高い領域に入る。既に `/api/mercari` で非公式 API に依存しているとはいえ、
書き込み自動化は一歩踏み込むことになる。**判断はトモヤさん本人のもの。**

## CDP から解放されるのか

正確には「**外部ブラウザの管理から解放される**」。ブラウザ操作そのものは残る。

| | 今 | Electron 内蔵後 |
|---|---|---|
| A / B（商品取得・クローンデータ） | ブラウザ不要（達成済み） | 変わらず不要 |
| C / D / E | 外部 Chrome + CDP + プロファイル管理 | アプリ内で完結 |
| chrome-devtools / playwright-ops | 必要 | 不要 |

## 今日の成果物の再利用先

| 成果物 | 再利用先 |
|---|---|
| `public/js/clone-service.js` | Electron のメインプロセスとレンダラの両方から読む |
| `mcp/server.mjs` | Electron のメインプロセスにそのまま載せる |
| `poc/obscura/src/browser-service.mjs` | 中身を Obscura → Electron の webContents に差し替えて流用 |
| `poc/obscura/src/obscura-process.mjs` | 不要（Electron が Chromium を内包するため） |

## 未決の判断

1. Electron か Tauri か（Tauri は WebView2/WKWebView を使うため、Windows と macOS で
   ブラウザ挙動が変わる。メルカリ操作の安定性では Electron が有利）
2. ~~Electron 化を先にやるか、フェーズ 1 を先に CDP でプロトタイプするか~~
   → **フェーズ1〜3 は CDP で完了済み。Electron 化をやると決定（2026-09-03）**
3. ~~書き込み自動化に踏み込むかどうか~~ → **踏み込み済み**（価格変更・下書き作成とも実運用）
4. 配布するかどうか。**しないなら署名・公証・自動更新は永久に不要**
